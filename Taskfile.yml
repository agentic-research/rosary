version: '3'

# Assembled from per-concern modules — Taskfile is the source of truth and CI
# hooks into these targets (never raw tool commands). `image:` owns the OCI
# build/publish path (`task image:build` / `image:release`).
includes:
  image: .taskfiles/image.yml

vars:
  # ley-line-open ships a fuse.pc wrapper that maps fuse-t for the fuser
  # crate. Adjust LEY_LINE_DIR if ley-line-open is checked out elsewhere.
  LEY_LINE_DIR: '{{.ROOT_DIR}}/../ley-line-open'
  FUSE_PKG_CONFIG_PATH:
    sh: 'if [ "$(uname)" = "Darwin" ]; then echo "{{.LEY_LINE_DIR}}/rs/pkgconfig"; fi'
  VERSION:
    sh: git describe --tags --always --dirty 2>/dev/null || echo dev
  UNAME_M:
    sh: uname -m

env:
  PKG_CONFIG_PATH: '{{.FUSE_PKG_CONFIG_PATH}}'
  # Disable inherited rustc wrappers (notably sccache) inside task recipes by
  # default. Sandboxed Codex verification cannot always execute those wrappers;
  # users who want one can still pass CARGO_BUILD_RUSTC_WRAPPER explicitly.
  CARGO_BUILD_RUSTC_WRAPPER: '{{.CARGO_BUILD_RUSTC_WRAPPER | default ""}}'

tasks:
  default:
    desc: Show available tasks
    cmds:
      - task --list

  build:
    desc: Build rsry binary (debug)
    cmds:
      - cargo build

  release:
    desc: Build rsry binary (release)
    cmds:
      - cargo build --release

  test:
    desc: Run all tests (Rust + Elixir)
    cmds:
      - cargo test

  compile:
    desc: Compile check without codegen
    cmds:
      - cargo check

  taskfile-contract:
    desc: Verify Taskfile remains the source of truth for CI/check gates
    cmds:
      - bash scripts/check-taskfile-contract.sh

  install-service-contract:
    desc: Verify the macOS rsry MCP launch-agent migration and restart contract
    cmds:
      - bash scripts/check-install-restart.sh
      - bash scripts/install-rsry-service.test.sh

  rules:
    desc: >-
      Rosary-domain mechanical gates that CI and dispatched agents must run.
      They live here (inside the canonical `task check`) rather than only in the
      local pre-commit hooks, because agents run `task check`, not pre-commit —
      so a pre-commit-only gate enforces nothing on the dispatch path.
      Structural code smells (god_file/long_file/dead_code/…) are NOT re-listed
      here: those are mache's rule engine (JSON-of-SQL over the AST + `--baseline`
      ratchet, ADR-0018), run via `task smells` locally and the reusable mache
      find-smells action in CI (.github/workflows/smells.yml) — not reinvented in
      bash. This target holds only the domain rules mache does not own.
    cmds:
      - task: install-service-contract
      # R4b — imperative persist_status() call sites may only decrease. Kept as a
      # rosary script (not a mache rule) because mache's call-extraction
      # under-counts it (node_refs sees 16 of the 21 real sites), so the bash
      # grep is the accurate ratchet for this specific migration.
      - task: persist-status-ratchet

  registry:build:
    desc: >-
      Build LLO's leyline-schema-bridge `tooldefs` capnp plugin (ADR-0006,
      rosary-08a278) into target/release. SHA-pinned git dep anchored in
      Cargo.toml; mirrors cloister's schema-bridge:build (PR #143). `cargo
      build -p leyline-schema-bridge --bin …` emits the plugin into the shared
      target/ so `capnp compile -o…` can invoke it as an argv[0]-dispatched
      plugin.
    cmds:
      - cargo build --release -p leyline-schema-bridge --bin capnpc-schema-bridge-tooldefs
    sources:
      - Cargo.toml
      - Cargo.lock
    generates:
      - target/release/capnpc-schema-bridge-tooldefs

  registry:generate:
    desc: >-
      Regenerate src/serve/tools.generated.json from schemas/registry.capnp via
      the tooldefs plugin. Run after editing registry.capnp, then commit the
      result. `task registry:check-drift` (in `task check`) fails until you do.
    deps: [registry:build]
    cmds:
      - |
        set -eu
        PLUGIN="$PWD/target/release/capnpc-schema-bridge-tooldefs"
        TMPD=$(mktemp -d)
        trap 'rm -rf "$TMPD"' EXIT
        # `capnp compile` exits 0 even when the plugin errors and writes
        # nothing — its stderr carries the real diagnostic. Generate into a
        # tmpdir + existence-check + atomic move so a failed run can't leave a
        # stale committed file silently passing (same shape as cloister's
        # cluster:zod). `-I schemas` resolves registry.capnp's
        # `import "/_traits.capnp"` against the vendored copy.
        capnp compile -I schemas -o"$PLUGIN":"$TMPD" schemas/registry.capnp
        if [ ! -f "$TMPD/registry.tools.json" ]; then
          echo "FAIL — tooldefs plugin produced no registry.tools.json; check capnp stderr above" >&2
          exit 1
        fi
        mv "$TMPD/registry.tools.json" src/serve/tools.generated.json
        echo "Regenerated src/serve/tools.generated.json"
    sources:
      - schemas/registry.capnp
      - schemas/_traits.capnp
      - target/release/capnpc-schema-bridge-tooldefs
    generates:
      - src/serve/tools.generated.json

  registry:check-drift:
    desc: >-
      Drift gate — regenerate the tool registry into a temp file and diff it
      against the committed src/serve/tools.generated.json. Fails if
      schemas/registry.capnp and the committed JSON diverge. Part of `task
      check`. Mirrors cloister's cluster:zod:check-drift (PR #143).
    deps: [registry:build]
    cmds:
      - |
        set -eu
        PLUGIN="$PWD/target/release/capnpc-schema-bridge-tooldefs"
        TMPD=$(mktemp -d)
        trap 'rm -rf "$TMPD"' EXIT
        capnp compile -I schemas -o"$PLUGIN":"$TMPD" schemas/registry.capnp
        if [ ! -f "$TMPD/registry.tools.json" ]; then
          echo "FAIL — tooldefs plugin produced no registry.tools.json; check capnp stderr above" >&2
          exit 1
        fi
        if ! diff -u src/serve/tools.generated.json "$TMPD/registry.tools.json"; then
          echo "" >&2
          echo "DRIFT — committed src/serve/tools.generated.json differs from what the" >&2
          echo "tooldefs plugin generates from schemas/registry.capnp today. Run" >&2
          echo "\`task registry:generate\` to regenerate, then commit." >&2
          exit 1
        fi
        echo "OK — tools.generated.json matches the registry.capnp codegen output"
    sources:
      - schemas/registry.capnp
      - schemas/_traits.capnp
      - target/release/capnpc-schema-bridge-tooldefs
      - src/serve/tools.generated.json

  smells:
    desc: >-
      mache structural-smell ratchet (ADR-0018) — the god_file/long_file/
      dead_code/duplicate/… gate, owned by mache's rule registry (JSON-of-SQL over
      the AST), NOT reinvented in bash. Builds a mache index over the repo and
      fails on findings that EXCEED the committed docs/smell-baseline.json (the
      per-(rule,file) ratchet floor). Custom rosary rules live as JSON in
      docs/smell-rules/ (loaded via MACHE_SMELL_RULES_DIR) — e.g. long_file_rosary
      is Golden Rule 2 (>500 lines), which retired scripts/check-file-length.sh.
      CI runs the identical gate via the reusable `agentic-research/mache`
      find-smells action; this target is the local mirror — keep the invocation
      (`--rule '*' --tags=gate --limit 100000 --baseline-root`) in sync with
      .github/workflows/smells.yml's pinned action. Requires mache >= v0.17.0
      (the `gate` rule tag; below that `--tags=gate` matches nothing).
      Rosary pins Mache, not leyline: the selected Mache release owns, verifies,
      and auto-provisions its exact compatible leyline binary.
      Regenerate the baseline after an intentional change with:
      `MACHE_SMELL_RULES_DIR=docs/smell-rules mache find-smells --db <db> --rule '*'
      --tags=gate --limit 100000 --baseline-root "$PWD"
      --write-baseline docs/smell-baseline.json`.
      NOTE (corrected 2026-07-27, rosary-e87edb): the "darwin disagrees with
      linux" warning that used to live here is FOLKLORE and was deleted. Three
      controlled builds from clean `git archive` exports showed ZERO disagreeing
      (rule,file) pairs: darwin+mache-v0.18.0 vs the linux-generated baseline,
      and darwin+mache-v0.19.0-10 vs the same. The rule JSON is byte-identical
      between v0.18.0 and mache HEAD. Regenerate from wherever you like.
      The real cause of "phantom" disagreements is that `duplicate_definitions`
      groups by token NAME globally with no file/module in the key, so ADDING
      `fn build` in a NEW file creates a finding in an UNRELATED file that the
      PR never opened. Hand-patching single entries therefore drifts; regenerate
      fully instead.
    env:
      MACHE_SMELL_RULES_DIR: '{{.TASKFILE_DIR}}/docs/smell-rules'
    cmds:
      - |
        if ! command -v mache >/dev/null 2>&1; then
          echo "SKIP: mache not on PATH — install mache >= v0.17.0 for the structural"
          echo "      smell gate. CI enforces it via the mache find-smells action."
          exit 0
        fi
        DB="$(mktemp -u).db"
        trap 'rm -f "$DB"' EXIT
        mache build . "$DB"
        mache find-smells --db "$DB" --rule '*' --tags=gate --limit 100000 \
          --baseline-root "$PWD" --baseline docs/smell-baseline.json

  # --------------------------------------------------------------------------
  # Mutation testing — the gate that checks the TESTS, not the code.
  #
  # Why this exists: a hand-written regression test for auto-close logic
  # (rosary-e0e19f) revealed what a mutation test catches instantly — the
  # wrong parsing path was green because the test was hand-rolled instead of
  # machine-generated. Later, rosary-cb9321 showed the real cost: the
  # first-dash id parser silently dropped closures when the split logic
  # subtly mutated. The whole test suite passed; the mutation test failed
  # immediately.
  #
  # Scoped to the pure parsers in src/vcs.rs (parse_merged_closures,
  # parse_merge_commit_record, extract_bracket_ids, valid_bead_id,
  # trailing_pr_number, extract_bead_refs, extract_bead_ids, github_slug)
  # via -F filter — the rest of vcs.rs spawns git and is not falsifiable
  # at the mutation level.
  #
  # GOTCHA for the next rung (found landing rosary-e97360): `-F` does not
  # filter out struct-literal-field-deletion mutants scattered through a
  # LARGE file — confirmed against src/main.rs, where ~20 such mutants in
  # unrelated functions (main, bootstrap_git_tracked_beads) survive even a
  # match-nothing -F pattern. vcs.rs never showed this because the whole
  # file is small enough that scope ≈ target. Prefer giving new pure logic
  # its own small file (see src/gitignore.rs) over trying to -F-scope it
  # inside main.rs or another god-file — `--file` alone is then sufficient
  # and the noise class doesn't exist.
  #
  # NOT in `task ci`: cargo mutants rebuilds per mutant (~2-3 min warm).
  # Runs on demand (`task mutants:<name>`) and in its own workflow
  # (.github/workflows/mutants.yml, path-filtered or explicit trigger).
  # `-j 1`, not 2: cargo-mutants gives each parallel worker its own scratch
  # build copy, so -j 2 pays for a second full dependency compile rather than
  # reusing the baseline build's warm target dir. On CI's runner that cost
  # (contending for CPU/RAM against the first worker) blew the 45-min budget
  # with zero completed mutants logged (PR #454, rosary-b2ae79 follow-up) even
  # though the same command finished locally. -j 1 reuses one scratch copy's
  # incremental build across all mutants in a file — slower in theory, faster
  # in practice here. Revisit if a file's mutant count grows enough to want
  # real parallelism; that needs a shared/warm target dir per worker, not just
  # a higher -j.
  # --------------------------------------------------------------------------
  mutants:
    desc: "Mutation-test every earned rung. Fails if any mutant survives."
    cmds:
      - task: mutants:vcs
      - task: mutants:gitignore
      - task: mutants:precommit_yaml

  mutants:vcs:
    desc: "Mutation-test src/vcs.rs pure parsers — the rung earned by rosary-e0e19f + rosary-cb9321."
    cmds:
      - >-
        cargo mutants --file src/vcs.rs
        -F 'parse_merged_closures|parse_merge_commit_record|extract_bracket_ids|valid_bead_id|trailing_pr_number|extract_bead_refs|extract_bead_ids|github_slug'
        -j 1 -- --bin rsry

  mutants:gitignore:
    desc: "Mutation-test src/gitignore.rs — the rung earned by rosary-e97360 (9-of-22-repo gitignore-shadow fleet sweep)."
    cmds:
      - cargo mutants --file src/gitignore.rs -j 1 -- --bin rsry

  mutants:precommit_yaml:
    desc: "Mutation-test src/precommit_yaml.rs — the rung earned by rosary-00f2b5 (pre-commit-framework hook durability, found live in mache)."
    cmds:
      - cargo mutants --file src/precommit_yaml.rs -j 1 -- --bin rsry

  coverage:
    desc: >-
      Test-coverage ratchet (rosary-69fdde) — the coverage analog of the mache
      smell baseline. Runs cargo-llvm-cov and fails on any per-file line-coverage
      regression below docs/coverage-baseline.json (the safety net that makes
      decomposition like rosary-6a143f safe). The number is ONLY ever the tool's
      output — regenerated and compared, never hand-typed — the structural guard
      against fabricated coverage (rosary-5732ee). Local-first: run before/after a
      decomposition to prove no regression. Skips (exit 0) when cargo-llvm-cov is
      absent. CI enforcement is a follow-up (rosary-ae05a5 — needs a CI-native
      baseline, since CI lacks dolt/jj and their tests skip).
    cmds:
      - |
        if ! cargo llvm-cov --version >/dev/null 2>&1; then
          echo "SKIP: cargo-llvm-cov not installed."
          echo "      cargo install cargo-llvm-cov && rustup component add llvm-tools-preview"
          exit 0
        fi
        COV="$(mktemp -u).json"
        trap 'rm -f "$COV"' EXIT
        cargo llvm-cov --json --output-path "$COV" --workspace
        python3 scripts/coverage-ratchet.py --check "$COV"

  coverage:baseline:
    desc: >-
      Regenerate docs/coverage-baseline.json from a fresh cargo-llvm-cov run.
      Run after an intentional coverage change (e.g. a split that moves lines) —
      never hand-edit the baseline.
    cmds:
      - |
        COV="$(mktemp -u).json"
        trap 'rm -f "$COV"' EXIT
        cargo llvm-cov --json --output-path "$COV" --workspace
        python3 scripts/coverage-ratchet.py --write "$COV"

  jj-lib-cost:
    desc: >-
      Falsifiable jj-lib adoption cost gate (rosary-efd300) — measures, with
      real numbers instead of inherited belief, what depending on jj-lib
      (directly, and via leyline-vcs) would cost rosary: net-new transitive
      crates, cold+warm release build time, and the `rsry` release binary
      size — each against an unmodified baseline built from the same commit.
      Settles whether the "heavy leyline-fs/jj-lib subtree" premise behind
      rosary-30374f (see the leyline-core comment in Cargo.toml) survives
      measurement. Runs entirely in scratch git worktrees + isolated
      CARGO_TARGET_DIRs and cleans up via `trap ... EXIT INT TERM` — never
      touches the committed Cargo.toml/Cargo.lock (the real working tree is
      byte-identical after any run, including Ctrl-C). NOT part of `task
      check`: a full run does 2-3 full release builds and is slow by design
      — run it deliberately when jj-lib adoption is actually on the table.
      `task jj-lib-cost -- --quick` does the dependency-count axis only
      (seconds, no builds). See scripts/jj-lib-cost-gate.sh for the declared
      budgets and how build-time noise is handled.
    cmds:
      - bash scripts/jj-lib-cost-gate.sh {{.CLI_ARGS}}

  check:
    desc: Canonical verification gate (Taskfile source of truth)
    cmds:
      - task: taskfile-contract
      - task: rules
      - task: compile
      - task: lint
      - task: test
      # Generated-tool-registry drift gate (ADR-0006, rosary-08a278): the
      # committed src/serve/tools.generated.json must match what the
      # leyline-schema-bridge tooldefs plugin regenerates from
      # schemas/registry.capnp. Hard (no SKIP): capnp is already a build
      # prereq (build.rs codegens schemas/cloister.capnp), so the CLI is
      # always present on any host that can build rosary.
      - task: registry:check-drift
      # server.json (hand-maintained MCP manifest) version must track Cargo.toml —
      # it silently drifted to 0.5.1 while releases went to 0.8.1 (rosary-7e4698).
      - task: server-json:check
      # Structural smell ratchet — enforced locally where mache is present, and in
      # CI via the reusable mache find-smells action (.github/workflows/smells.yml).
      # Skips (exit 0) when mache is absent, so the mache-less CI `verify` job that
      # runs `task check` doesn't fail; the action is the CI enforcement point.
      - task: smells

  lint:
    desc: Run fmt + clippy + semgrep rules
    deps: [fmt]
    cmds:
      - cargo clippy -- -D warnings
      # Rule QA first: `--validate` (well-formed) + `--test` (fixtures still
      # match) so a rotted rule fails loudly before it silently stops catching
      # things (rosary-4b18f0). Then the scan. Both degrade cleanly when
      # semgrep is present but unusable (X509 trust-init in a sandbox,
      # rosary-9e5138); still fail on real findings/failures.
      - bash scripts/check-semgrep-rules.sh .semgrep/rules.yml .semgrep/rules.rs
      - bash scripts/run-semgrep.sh .semgrep/rules.yml .

  fmt:
    desc: Format code
    cmds:
      - cargo fmt

  server-json:check:
    desc: "server.json (hand-maintained MCP manifest) must match Cargo.toml version AND the registered tool set"
    silent: true
    cmds:
      - |
        set -euo pipefail
        # 1. version must track Cargo.toml (both the top-level and the OCI package).
        CARGO="$(awk -F'"' '/^version = /{print $2; exit}' Cargo.toml)"
        TOP="$(jq -r '.version' server.json)"
        PKG="$(jq -r '.packages[0].version' server.json)"
        if [ "$TOP" != "$CARGO" ] || [ "$PKG" != "$CARGO" ]; then
          echo "server.json version drift: Cargo.toml=$CARGO server.json.version=$TOP packages[0].version=$PKG — reconcile to $CARGO"
          exit 1
        fi
        # 2. the standalone tool list must equal the tools the dispatcher registers.
        # server.json is hand-maintained (no generator yet — rosary-1a4c0b/ADR-0006),
        # so this gate is what stops it silently drifting (it had dropped
        # rsry_expand_ref). Source of truth: the dispatch match arms in handlers.
        ACTUAL="$(grep -oE '"rsry_[a-z_]+" =>' src/serve/handlers/mod.rs | grep -oE 'rsry_[a-z_]+' | sort -u)"
        LISTED="$(jq -r '._meta["io.github.agentic-research.rosary/tools"].standalone[]' server.json | sort -u)"
        AF="$(mktemp)"; LF="$(mktemp)"
        printf '%s\n' "$ACTUAL" > "$AF"; printf '%s\n' "$LISTED" > "$LF"
        if ! diff -q "$AF" "$LF" >/dev/null; then
          echo "server.json tool drift vs src/serve/handlers/mod.rs dispatch arms:"
          echo "  missing from server.json: $(comm -23 "$AF" "$LF" | tr '\n' ' ')"
          echo "  stale in server.json:     $(comm -13 "$AF" "$LF" | tr '\n' ' ')"
          rm -f "$AF" "$LF"
          exit 1
        fi
        rm -f "$AF" "$LF"
        echo "server.json OK: version $CARGO, $(printf '%s\n' "$ACTUAL" | wc -l | tr -d ' ') tools match handlers"

  persist-status-ratchet:
    desc: "Imperative persist_status() call sites may only decrease (R4b — state = fold, not scattered mutation)"
    cmds:
      - bash scripts/check-persist-status-ratchet.sh

  god-files:
    desc: >-
      Report god-files (structural view for the R6 breakdown campaign) via mache's
      long_file/god_file rules. Reporting only — the enforced gate is `task smells`
      (ratcheted against docs/smell-baseline.json). The old wc -l script
      (check-god-files.sh) was retired: it duplicated mache's long_file (>1500).
    cmds:
      - |
        if command -v mache >/dev/null 2>&1; then
          DB="$(mktemp -u).db"
          trap 'rm -f "$DB"' EXIT
          mache build . "$DB" >/dev/null 2>&1 && {
            echo "=== mache long_file (>1500) + god_file ==="
            mache find-smells --db "$DB" --rule long_file --format ci 2>/dev/null || true
            mache find-smells --db "$DB" --rule god_file  --format ci 2>/dev/null || true
          }
        else
          echo "SKIP: mache not on PATH — install for the structural view"
        fi

  install:
    desc: Build, codesign, install rsry, and set up HTTP MCP service
    # Compose from the named release + lint tasks rather than reinventing
    # them inline. `release` produces the binary; `lint` (clippy -D warnings,
    # via its fmt dep) gates the install so we don't ship a binary that
    # wouldn't pass CI. Both are cached by taskfile if they ran in the
    # same invocation, so `task check && task install` doesn't double-build.
    deps: [release, lint]
    cmds:
      - |
        # Check for existing rsry in PATH (brew, cargo install, system package)
        EXISTING=$(command -v rsry 2>/dev/null || true)
        if [ -n "$EXISTING" ] && [ "$EXISTING" != "$HOME/.local/bin/rsry" ]; then
          EXISTING_VER=$("$EXISTING" --version 2>/dev/null || echo "unknown")
          NEW_VER=$(cargo metadata --format-version 1 --no-deps 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin)['packages'][0]['version'])" 2>/dev/null || echo "dev")
          echo "WARNING: found rsry at $EXISTING ($EXISTING_VER)"
          echo "         task install will place rsry at ~/.local/bin/rsry ($NEW_VER)"
          # Check if it's a brew install specifically
          if echo "$EXISTING" | grep -q "homebrew\|Cellar"; then
            echo "         Suggestion: brew unlink rosary"
          fi
          echo ""
          read -p "Continue? [Y/n] " -n 1 -r
          echo
          if [[ ! $REPLY =~ ^[Yy]?$ ]]; then
            echo "Aborted."
            exit 1
          fi
        fi
      - mkdir -p ~/.local/bin ~/.rsry
      # Stage + codesign in /tmp, then atomic rename to install path.
      # This ensures WatchPaths sees exactly one change (the rename),
      # not two (cp then codesign modifying in-place).
      - cp target/release/rsry /tmp/rsry-staged
      - codesign -s - --force --deep --entitlements entitlements.plist /tmp/rsry-staged
      - mv /tmp/rsry-staged ~/.local/bin/rsry
      # Restart is handled by install-service via `launchctl kickstart -k`
      # (rosary-de1237) — WatchPaths/touch didn't reliably fire on the
      # atomic-rename inode swap, so the service ran stale for days.
      - echo "Installed rsry to ~/.local/bin/rsry (codesigned)"
      - task: install-service
      # Hook templates are compiled into the binary. Refresh them in the same
      # install transaction so a newly shipped guard cannot remain dormant
      # behind an older generated hook (rosary-c9f1ee).
      - ~/.local/bin/rsry hooks --repo . install

  install-service:
    desc: Install/update launchd service for rsry HTTP MCP server (macOS)
    platforms: [darwin]
    cmds:
      - bash scripts/install-rsry-service.sh

  overnight:
    desc: Start overnight loop (concurrency=1, bugs first, caffeinate)
    deps: [install]
    cmds:
      - |
        caffeinate -dims &
        echo "Caffeinate PID: $!"
        nohup rsry run --config rosary-self.toml --overnight > /tmp/rsry-overnight.log 2>&1 &
        echo "Rosary PID: $!"
        echo "Log: /tmp/rsry-overnight.log"
        echo "Stop: kill $(cat /tmp/rsry-overnight.pid 2>/dev/null || echo $!)"
        tail -f /tmp/rsry-overnight.log

  e2e:
    desc: End-to-end sandbox test (requires Dolt server in .beads/)
    deps: [build]
    cmds:
      - ./scripts/e2e-sandbox.sh

  e2e:fresh:
    desc: Fresh-setup regression test — vanilla Ubuntu, prereqs, build, smoke
    # Builds e2e.Dockerfile and runs scripts/e2e-fresh-setup.sh inside the
    # container. Mirrors the documented "first-time user" path on a clean
    # machine — asserts that `rsry enable` rejects a missing dolt identity
    # without half-initializing .beads/, and that no migration-noise
    # regression has snuck back in.
    cmds:
      - docker build -f e2e.Dockerfile -t rosary-e2e .
      - docker run --rm rosary-e2e

  integration:
    desc: Rust CLI integration tests (requires dolt in PATH)
    deps: [build]
    cmds:
      - cargo test --test cli_integration -- --test-threads=1

  apk:
    desc: "[deferred] Build distroless OCI image via melange + apko (release)"
    # Kept for reference; the active path is `task image` (krust + image.Dockerfile)
    # — adopted from ley-line-open after its melange path stalled on Apple Silicon
    # (ley-line-open-2b255c). Bind-mounting this repo's 31GB target/ into a melange
    # docker runner makes the `populating workspace` phase pathological.
    cmds:
      - melange keygen melange.rsa
      - |
        melange build melange.yaml \
          --arch {{.UNAME_M}} \
          --signing-key melange.rsa \
          --out-dir ./packages
      - |
        apko build apko.yaml \
          rosary:{{.VERSION}} \
          rosary.tar \
          --keyring-append melange.rsa.pub
      - echo "Built rosary.tar (load with docker load < rosary.tar)"

  # Image build/publish moved to `.taskfiles/image.yml` (included below under the
  # `image:` namespace): `task image:build` / `image:smoke` / `image:release`.

  setup:
    desc: First-time setup — install git hooks, conductor deps
    cmds:
      - |
        if command -v pre-commit >/dev/null 2>&1; then
          pre-commit install
          pre-commit install --hook-type commit-msg
          echo "✓ Git hooks installed (pre-commit + commit-msg)"
        else
          echo "SKIP: pre-commit not found (brew install pre-commit)"
        fi
      - |
        if command -v rsry >/dev/null 2>&1; then
          rsry enable .
          echo "✓ Repo registered with rosary"
        else
          echo "SKIP: rsry not found (brew install rosary — coming soon)"
        fi

  all:
    desc: Alias for the canonical verification gate
    cmds:
      - task: check

  ci:
    desc: CI alias for the canonical verification gate
    # CI is secondary to this Taskfile: GitHub Actions delegates here, and this
    # target delegates to `check` instead of duplicating raw tool commands.
    cmds:
      - task: check
